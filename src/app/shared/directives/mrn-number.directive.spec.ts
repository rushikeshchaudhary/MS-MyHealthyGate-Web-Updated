import { MrnNumberDirective } from './mrn-number.directive';

describe('MrnNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new MrnNumberDirective();
    expect(directive).toBeTruthy();
  });
});
